# Assessment CSV field definitions

Every template contains a synthetic example row. `id` and all reference fields use stable lowercase identifiers. Semicolon-delimited cells represent lists. `evidence` references the evidence register. `Unknown`, `Evidence unavailable`, and `Requires review` are valid and must not be replaced with invented answers. Times are minutes; percentages are whole numbers. Importers must reject duplicates and unresolved references.
